import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.primeraevaluacion',
  appName: 'primeraevaluacion',
  webDir: 'www'
};

export default config;
